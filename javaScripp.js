/** @format */

const operdador = 2;
let operando = 1;
let result = 0;

result = operdador * operando;
console.log(result)



operando = operando +1
result = operdador * (operando )
console.log(result)

operando = operando +1
result = operdador * (operando)
console.log(result)

operando = operando +1
result = operdador * (operando)
console.log(result)

operando = operando +1
result = operdador * (operando)
console.log(result)

operando = operando +1
result = operdador * (operando)
console.log(result)

operando = operando +1
result = operdador * (operando)
console.log(result)

operando = operando +1
result = operdador * (operando)
console.log(result)
